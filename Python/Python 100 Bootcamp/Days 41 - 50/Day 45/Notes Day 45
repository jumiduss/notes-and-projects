HTML Parsing Using Beautiful Soup
    from bs4 import BeautifulSoup # Beautiful Soup v 4
    import lxml # HTML Parser
    
    with open("file_name.extension_name",:"r") as file:
        context = file.read()
    
    soup = BeautifulSoup(context, "lxml")
    all_anchor_tags = soup.find_all(name="a") # a represents the anchor tag's name

    for tag in all_anchor_tags:
        tag.get("href") #property of the anchor tag
    
    heading = soup.find(name="h1", id="name")